package io.code.geek_vs_geek.service;

import org.springframework.stereotype.Service;

// import org.springframework.beans.factory.annotation.Autowired;
//import org.springframework.messaging.simp.SimpMessagingTemplate;

@Service
public class TestCaseService {

    
    // @Autowired
    // private SimpMessagingTemplate messagingTemplate;

    // public void runTestCases(String sessionId, String code) {
    //     // Mock execution of test cases
    //     int passedTestCases = 3; // Example: assuming 3 test cases passed
    //     String message = "User passed " + passedTestCases + " test cases";

    //     // Send the message to the WebSocket topic
    //     messagingTemplate.convertAndSend("/topic/session/" + sessionId, message);
    // }
}

